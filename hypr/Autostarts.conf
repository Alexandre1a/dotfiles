#################
### AUTOSTART ###
#################

# Autostart necessary processes ( like notification daemons, status bars, etc...)
# Or autostart your favorites apps at launch
exec-once = ags
exec-once = /usr/lib/polkit-kde-authentication-agent-1 # The autentication agent
exec-once = swww-daemon --format xrgb &

# Initialises cliphist
exec-once = wl-paste --type text --watch cliphist store # Stores only text data
exec-once = wl-paste --type image --watch cliphist store # Stores only image data

# exec-once = nwg-panel
# exec-once = coolercontrol &

exec-once = $menu2

#exec-once = $scriptDir/ags.sh # HyprPanel (bar, panel and notification daemon)
